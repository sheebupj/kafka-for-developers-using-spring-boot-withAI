spring:
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:prod-kafka:9092}

library:
  events:
    topic: ${LIBRARY_EVENTS_TOPIC:library-events}

logging:
  level:
    root: WARN
    com.paremal.kafka: INFO
